Thumbs.db
.DS_Store

# Local visual and browser-test artifacts
*-preview.png
sample-*.jpg
browser-smoke.ps1

# Arduino compiler intermediates (the final .bin is kept)
firmware/**/.build/
firmware/**/build/
